<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#0F172A">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Driver Documents"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Keep your documents verified &amp; up to date"
            android:textColor="#94A3B8"
            android:layout_marginTop="4dp"/>

        <!-- Progress Bar (Added Style) -->
        <ProgressBar
            android:id="@+id/documentProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="12dp"
            android:layout_marginTop="16dp"
            android:progress="66"
            android:progressDrawable="@drawable/progress_bar_custom"/>

        <!-- Labels Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="2 of 3 verified"
                android:textColor="#94A3B8"
                android:textSize="14sp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="66% complete"
                android:textColor="#22C55E"
                android:textStyle="bold"
                android:textSize="14sp"/>
        </LinearLayout>

        <!-- Document Cards -->
        <include layout="@layout/item_document_verified" />
        <include layout="@layout/item_document_warning" />
        <include layout="@layout/item_document_missing" />

    </LinearLayout>
</ScrollView>